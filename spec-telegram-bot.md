# Спецификация: Telegram-бот подрядчика

## 1. Назначение

Telegram-бот для взаимодействия с подрядчиками: получение план-наряда, ввод факта выполнения, уведомления. Минимальный интерфейс — 2–3 кнопки, без сложных форм.

**Стек:** Python (aiogram 3.x) + Express API (рапорт) + MongoDB (сессии бота)

---

## 2. Регистрация и авторизация

### 2.1. Сценарий первого входа

```
Подрядчик → /start → Бот: «Введите код доступа»
                      ↓
Код = генерируется в рапорте (admin → «Создать код для подрядчика»)
                      ↓
Бот проверяет код через API → привязывает telegram_id к contractor_id
                      ↓
«Вы авторизованы как {contractor_name}, объект {housing_name}»
```

### 2.2. Структура авторизации

```
ContractorBotUser {
  telegram_id:      BIGINT       // Telegram user ID
  telegram_username: STRING
  contractor_id:    UUID         // из рапорта
  housing_ids:      [UUID]       // объекты (может быть несколько)
  registered_at:    TIMESTAMP
  is_active:        BOOLEAN
  language:         STRING       // 'ru' (по умолчанию)
  role:             ENUM         // 'foreman' | 'master' | 'engineer'
}
```

Хранение: **MongoDB** (коллекция `bot_users`)

### 2.3. Код доступа

```
AccessCode {
  code:             STRING       // 6 цифр, одноразовый
  contractor_id:    UUID
  housing_ids:      [UUID]
  created_by:       UUID         // кто выдал (РС / администратор)
  created_at:       TIMESTAMP
  expires_at:       TIMESTAMP    // TTL: 72 часа
  used:             BOOLEAN
  used_by:          BIGINT?      // telegram_id
}
```

Хранение: **MongoDB** (коллекция `access_codes`, TTL index на `expires_at`)

---

## 3. Команды и интерфейс

### 3.1. Главное меню

```
/start       — регистрация / главное меню
/plan        — план-наряд на сегодня
/report      — отчитаться за сегодня
/history     — история за неделю
/help        — справка
/settings    — настройки (язык, уведомления)
```

### 3.2. Inline-кнопки (основной UX)

**Главное меню (после авторизации):**
```
📋 План на сегодня     📝 Отчитаться
📊 Моя статистика      ⚙️ Настройки
```

### 3.3. Сценарий: Просмотр план-наряда (/plan)

```
Пользователь нажимает «📋 План на сегодня»
    ↓
Бот: GET /api/v1/plan-naryad/contractor/{contractor_id}?date=today
    ↓
📋 План-наряд на 11.03.2026

Объект: ЖК «Первый Донской», Корп. 4

1️⃣ Секция 1, этаж 3
   Штукатурка стен — 120 м²

2️⃣ Секция 1, этаж 4
   Штукатурка стен — 85 м²

3️⃣ Секция 2, этаж 3
   Стяжка пола — 60 м²

Итого: 3 задания
Дедлайн отчёта: 20:00

[📝 Отчитаться сейчас]
```

### 3.4. Сценарий: Ввод факта (/report)

**Принцип: максимально простой ввод. Два режима:**

**Режим 1: «Всё сделано» (fast track)**
```
Пользователь: «📝 Отчитаться»
    ↓
Бот показывает список заданий:

1️⃣ Штукатурка, С1 эт.3, 120 м²
2️⃣ Штукатурка, С1 эт.4, 85 м²
3️⃣ Стяжка, С2 эт.3, 60 м²

[✅ Всё выполнено]  [📝 По позициям]
```

Если «✅ Всё выполнено» → факт = план по всем позициям. Готово.

**Режим 2: «По позициям» (детальный ввод)**
```
Бот: Позиция 1 из 3
Штукатурка стен | С1, этаж 3 | План: 120 м²

Сколько выполнено? (введите число)

[✅ 120 (всё)] [⬜ 0 (не делали)] [📝 Другой объём]
```

Если «📝 Другой объём» → бот ждёт число от пользователя.

```
Пользователь: 80
    ↓
Бот: Штукатурка, С1 эт.3: 80 из 120 м² (67%)
     Верно?
     [✅ Да] [🔄 Исправить]
    ↓
Бот: Позиция 2 из 3...
```

После всех позиций:
```
📊 Отчёт за 11.03.2026

✅ Штукатурка, С1 эт.3: 80/120 м² (67%)
✅ Штукатурка, С1 эт.4: 85/85 м² (100%)
⬜ Стяжка, С2 эт.3: 0/60 м² (не делали)

Общий % выполнения: 62%

[📤 Отправить отчёт] [🔄 Исправить]
```

### 3.5. Сценарий: Внеплановая работа

```
После отправки отчёта:

Бот: Отчёт отправлен ✅
     Выполняли ещё что-то, чего не было в плане?

     [➕ Да, добавить] [Нет, всё]
```

Если «➕ Да»:
```
Выберите вид работ:
[Штукатурка] [Стяжка] [Электрика] [Сантехника] [...]
    ↓
Где? 
[С1] [С2] [С3]  →  [Эт.1] [Эт.2] [...] [Эт.25]
    ↓
Объём? (введите число, ед.изм.: м²)
    ↓
Добавлено: Электрика, С2 эт.5, 30 м²
```

### 3.6. Сценарий: Статистика

```
📊 Ваша статистика за неделю (03.03 – 10.03)

Объект: ЖК «Первый Донской», Корп. 4

Средний % выполнения: 78%
Факт подан в срок: 5 из 6 дней (83%)

По дням:
Пн 03.03: 85% ✅
Вт 04.03: 72% 🟡
Ср 05.03: 90% ✅
Чт 06.03: 60% ⚠️
Пт 07.03: 80% ✅
Сб 08.03: — (выходной)
```

---

## 4. Уведомления (outbound)

### 4.1. Расписание push-сообщений

| Время | Сообщение | Условие |
|-------|-----------|---------|
| ~10:01 | A03: План-наряд на сегодня | После подтверждения РС (или авто-подтверждения) |
| 18:00 | A04: Напоминание | Если факт не подан |
| — | Итог дня | После подачи факта (подтверждение) |

### 4.2. Формат A03

```
🏗️ Задание на {date}

Объект: {housing_name}

• С1, эт.3: Штукатурка стен — 120 м²
• С1, эт.4: Штукатурка стен — 85 м²
• С2, эт.3: Стяжка пола — 60 м²

Итого: 3 задания | Отчёт до 20:00

[📋 Подробнее] [📝 Отчитаться]
```

### 4.3. Формат A04 (напоминание)

```
⏰ Напоминание

По объекту {housing_name} вы ещё не подали факт.
Заданий: 3 | Дедлайн: 20:00

[📝 Отчитаться сейчас]
```

---

## 5. Архитектура

### 5.1. Компоненты

```
┌─────────────────┐     ┌──────────────────┐     ┌─────────────┐
│  Telegram API   │◄───▶│  Bot Service     │◄───▶│ Raport API  │
│  (Telegram)     │     │  (Python/aiogram)│     │ (Express)   │
└─────────────────┘     └──────────────────┘     └─────────────┘
                              │                        │
                              ▼                        ▼
                        ┌──────────┐            ┌──────────┐
                        │ MongoDB  │            │  MSSQL   │
                        │(сессии,  │            │(данные   │
                        │ коды)    │            │ рапорта) │
                        └──────────┘            └──────────┘
```

### 5.2. Взаимодействие с API рапорта

Бот использует **существующие** эндпоинты + **новые** из спецификации автогенерации:

| Действие бота | API эндпоинт | Метод |
|---------------|-------------|-------|
| Получить план-наряд | `/plan-naryad/contractor/{id}?date=` | GET |
| Отправить факт | `/work-facts` | POST |
| Получить структуру объекта | `/housings/{id}/structure` | GET |
| Получить виды работ | `/work-groups` | GET |
| Статистика | `/reconciliation/contractor/{id}?date_from=&date_to=` | GET |
| Авторизация (проверка кода) | `/bot/verify-code` (новый) | POST |

### 5.3. Новые API эндпоинты (для бота)

```
POST /api/v1/bot/access-codes
     body: { contractor_id, housing_ids, created_by }
     → { code: "123456", expires_at: "..." }

POST /api/v1/bot/verify-code
     body: { code: "123456", telegram_id: 123456789 }
     → { contractor_id, housing_ids, contractor_name }

GET  /api/v1/bot/contractor/{telegram_id}/status
     → { is_registered, contractor_name, housing_names }
```

---

## 6. Состояние и FSM

### 6.1. State Machine подрядчика

```
IDLE
  ├─ /plan → VIEWING_PLAN → IDLE
  ├─ /report → REPORTING
  │    ├─ «Всё выполнено» → CONFIRM_ALL → (POST facts) → IDLE
  │    └─ «По позициям» → REPORT_ITEM_1
  │         ├─ ввод объёма → REPORT_ITEM_2
  │         ├─ ... → REPORT_ITEM_N
  │         └─ → REVIEW_REPORT → (POST facts) → ASK_UNPLANNED
  │              ├─ «Да» → ADD_UNPLANNED → IDLE
  │              └─ «Нет» → IDLE
  ├─ /history → VIEWING_HISTORY → IDLE
  └─ /settings → SETTINGS → IDLE
```

### 6.2. Хранение состояния

MongoDB, коллекция `bot_sessions`:

```
BotSession {
  telegram_id:     BIGINT
  state:           STRING        // текущее состояние FSM
  context: {
    date:          DATE
    housing_id:    UUID
    plan_items:    [PlanItem]     // загруженный план
    current_index: INT           // текущая позиция (для пошагового ввода)
    fact_items:    [FactDraft]    // введённые факты (черновик)
  }
  updated_at:      TIMESTAMP
  ttl:             TIMESTAMP     // auto-expire через 24 часа
}
```

---

## 7. Обработка ошибок

| Ситуация | Ответ бота |
|----------|-----------|
| Нет плана на сегодня | «📋 На сегодня заданий нет. Хорошего дня!» |
| Факт уже подан | «✅ Вы уже отчитались за {date}. Хотите изменить? [Да] [Нет]» |
| Неверный формат объёма | «Введите число. Например: 80 или 120.5» |
| Объём больше плана ×2 | «⚠️ Вы указали {value}, план был {plan}. Уверены? [Да] [Исправить]» |
| API рапорта недоступен | «⚠️ Система временно недоступна. Попробуйте через 5 минут» |
| Не авторизован | «Введите код доступа. Получите его у руководителя строительства» |
| Код просрочен | «Код истёк. Запросите новый у руководителя» |

---

## 8. Мультиобъектность

Если подрядчик работает на нескольких объектах:

```
Бот: На каком объекте отчитаться?

[ЖК «Первый Донской», Корп.4]
[ЖК «Архитектор», Корп.2]
```

После выбора — стандартный flow.

---

## 9. Метрики бота

| Метрика | Как считать |
|---------|------------|
| MAU (Monthly Active Users) | Уникальные telegram_id за 30 дней |
| Факт через бот / через веб | source='contractor_bot' vs 'contractor_web' |
| Среднее время ввода факта | submitted_at − first_interaction_at |
| Конверсия: план получен → факт подан | count(facts) / count(plans_sent) |
| Время подачи факта | Распределение submitted_at (до/после 20:00) |

---

## 10. Деплой

- **Python 3.11+**, aiogram 3.x
- **MongoDB** — отдельная коллекция (или та же БД, что и рапорт, если MongoDB уже используется)
- **Webhook mode** (не polling) — для продакшена
- **Docker** — отдельный контейнер `raport-tg-bot`
- **Env vars:**
  - `TELEGRAM_BOT_TOKEN` — токен от BotFather
  - `RAPORT_API_URL` — базовый URL API рапорта
  - `RAPORT_API_KEY` — сервисный ключ для API
  - `MONGODB_URI` — строка подключения
  - `WEBHOOK_URL` — URL для Telegram webhook
